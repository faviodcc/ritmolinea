import type { Player } from '@/types/game'; import { avatarEmoji } from '@/lib/utils';
export function Leaderboard({players}:{players:Player[]}){const sorted=[...players].sort((a,b)=>b.score-a.score||b.exact_hits-a.exact_hits);return <aside className="leaderboard glass"><h3>Ranking parcial</h3>{sorted.map((p,i)=><div className="rankRow" key={p.id}><span className="rankNo">{i+1}</span><span className="rankAvatar">{avatarEmoji(p.avatar_id)}</span><span className="rankName"><strong>{p.name}</strong><small>{p.exact_hits} exactas · racha {p.streak}</small></span><span className="rankScore">{p.score}</span></div>)}</aside>}
