import { NextRequest, NextResponse } from 'next/server'; import { requireAdmin } from '@/lib/auth'; import { apiError } from '@/lib/http'; import { getSupabaseAdmin } from '@/lib/supabase/admin';
export async function GET(request:NextRequest){try{await requireAdmin(request);const{data,error}=await getSupabaseAdmin().from('songs').select('title,artist,release_year,decade,genre,country,spotify_url,difficulty,image_url,tags,is_active').order('artist');if(error)throw error;return new NextResponse(JSON.stringify(data??[],null,2),{headers:{'Content-Type':'application/json','Content-Disposition':'attachment; filename="ritmolinea-biblioteca.json"'}});}catch(error){return apiError(error);}}
